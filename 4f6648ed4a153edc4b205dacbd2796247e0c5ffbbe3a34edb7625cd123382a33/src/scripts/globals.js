// Game-wide
var zoomLevels = [0.5, 0.5, 0.5, 0.5, 0.5, 0.5, 0.5];
var windows = [];
var darkmodeFlag = false;

// Console
var boardFlag = 1;
var fullscreenFlag = false;

module.exports = {
    zoomLevels,
    windows,
    darkmodeFlag,
    boardFlag,
    fullscreenFlag,
};